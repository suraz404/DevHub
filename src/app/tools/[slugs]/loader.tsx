import { LoadingState } from "@/components/common/LoadingState";
import React from "react";

const Loader = () => {
  return (
    <div>
      <LoadingState />
    </div>
  );
};

export default Loader;
